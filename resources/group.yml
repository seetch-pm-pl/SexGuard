---
# The default group is used by default for all non-inscribed groups, as well as for all players without full access when communication with PurePerms, UniversalGroup and SexGroup is disabled.
# Deleting this item may cause the plugin to malfunction.
default:
  max-size: 5000
  max-count: 4
  ignored-flags: []
  ignored-regions: []
...